# Git
.git

# Build output (JAR built inside Docker)
target/

# Environment files
.env
.env.*
!.env.example

# Docker Compose
docker-compose*.yml

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# Local artifacts. Note docs/ is NOT excluded: the dashboard serves a few of those
# files from the classpath, so they must reach the build context.
showcase-output/
docs/PROGRESS.log
